const randomColor = ["blue", "green", "red", "black", "yellow", "silver",]
const random = () => {
   return color = randomColor[Math.floor(randomColor.length * Math.random() )]
//    return randomColor
}

module.exports = random